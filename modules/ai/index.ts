import { defineFeatureModule } from '../module-manifest';

export const AI_MODULE = defineFeatureModule({
  name: 'ai',
  description: 'AI draft generation, deterministic pricing, and provider adapters',
});
