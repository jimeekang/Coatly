import { defineFeatureModule } from '../module-manifest';

export const SCHEDULE_MODULE = defineFeatureModule({
  name: 'schedule',
  description: 'Job scheduling, calendar sync, and public holiday rules',
});
