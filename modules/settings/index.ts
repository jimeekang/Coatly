import { defineFeatureModule } from '../module-manifest';

export const SETTINGS_MODULE = defineFeatureModule({
  name: 'settings',
  description: 'Business profile, billing settings, and onboarding profile helpers',
});
