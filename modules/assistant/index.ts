import { defineFeatureModule } from '../module-manifest';

export const ASSISTANT_MODULE = defineFeatureModule({
  name: 'assistant',
  description: 'Workspace assistant interactions and dashboard guidance',
});
