import { defineFeatureModule } from '../module-manifest';

export const ONBOARDING_MODULE = defineFeatureModule({
  name: 'onboarding',
  description: 'New user onboarding flow and setup guidance',
});
