# Detailed Estimate Anchors Implementation Plan

> For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox syntax for tracking.

Goal: Add user-configurable Detailed Estimate room anchors in Price Rates and
make New Quote > Detailed Estimate use them while keeping Room flat rate
separate.

Architecture: Store the new setting in businesses.default_rates JSON through
the existing UserRateSettings flow. Normalize detailed room anchors into cents
before calculation. Keep the pricing engine boundary in lib/interior-estimates.ts
so both client preview and server create/update paths use the same behaviour.

Tech Stack: Next.js App Router, React 19, TypeScript, Supabase, Zod, Vitest,
Testing Library, Tailwind.

---

## File Map

- Modify: lib/rate-settings.ts
  - Add detailed estimate anchor types, schema, defaults, parsing, and helpers.
- Create: lib/detailed-estimate-anchors.ts
  - Own the editable detailed-estimate anchor shape and default conversion from
    config/interior-estimate-anchors.ts.
- Modify: lib/interior-estimates.ts
  - Read user-configurable room anchors for specific-area interior estimates.
- Modify: components/rates/PriceRatesForm.tsx
  - Add Detailed Estimate room anchor UI under Price Rates.
- Modify: components/quotes/QuoteForm.tsx
  - Add a small Price Rates note/link for Detailed Estimate.
- Test: lib/rate-settings.test.ts
  - Cover parse/default behaviour for detailed_estimate_anchors.
- Test: lib/interior-estimates.test.ts
  - Cover custom anchor calculation and fallback.
- Test: components/rates/PriceRatesForm.test.tsx
  - Cover the new Price Rates Detailed Estimate Anchors editor.
- Test: components/quotes/QuoteForm.test.tsx
  - Cover UI note/link and separation from room flat rate.
- Test: app/actions/quotes.test.ts
  - Cover server-side create/update custom anchor totals.

## Task 1: Add Rate Setting Types And Defaults

Files:
- Create: lib/detailed-estimate-anchors.ts
- Modify: lib/rate-settings.ts
- Test: lib/rate-settings.test.ts

- [ ] Step 1: Write failing parser/default tests.

Add tests that check:

```ts
import {
  DEFAULT_RATE_SETTINGS,
  parseUserRateSettings,
} from '@/lib/rate-settings';

it('parses detailed estimate room anchors from default rates json', () => {
  const parsed = parseUserRateSettings({
    detailed_estimate_anchors: {
      interior_rooms: {
        'Bedroom 1': { min: 100000, median: 120000, max: 140000 },
      },
    },
  });

  expect(parsed.detailed_estimate_anchors.interior_rooms['Bedroom 1']).toEqual({
    min: 100000,
    median: 120000,
    max: 140000,
  });
});

it('falls back to default detailed estimate anchors when missing', () => {
  const parsed = parseUserRateSettings({});

  expect(parsed.detailed_estimate_anchors.interior_rooms['Bedroom 1'])
    .toEqual(DEFAULT_RATE_SETTINGS.detailed_estimate_anchors.interior_rooms['Bedroom 1']);
});
```

- [ ] Step 2: Run the focused test and confirm it fails.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run lib/rate-settings.test.ts
```

Expected: FAIL because detailed_estimate_anchors does not exist yet.

- [ ] Step 3: Add the shared anchor helper file.

Create lib/detailed-estimate-anchors.ts:

```ts
import { INTERIOR_ESTIMATE_ANCHORS } from '@/config/interior-estimate-anchors';

export const DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS = Object.keys(
  INTERIOR_ESTIMATE_ANCHORS.apartment.specific_areas.rooms
) as Array<keyof typeof INTERIOR_ESTIMATE_ANCHORS.apartment.specific_areas.rooms>;

export type DetailedEstimateInteriorRoom =
  (typeof DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS)[number];

export type DetailedEstimateAnchorRange = {
  min: number;
  median: number;
  max: number;
};

export type DetailedEstimateAnchors = {
  interior_rooms: Record<DetailedEstimateInteriorRoom, DetailedEstimateAnchorRange>;
};

export function buildDefaultDetailedEstimateAnchors(): DetailedEstimateAnchors {
  return {
    interior_rooms: Object.fromEntries(
      DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS.map((room) => {
        const range = INTERIOR_ESTIMATE_ANCHORS.apartment.specific_areas.rooms[room];

        return [
          room,
          {
            min: Math.round(range.min * 100),
            median: Math.round(range.median * 100),
            max: Math.round(range.max * 100),
          },
        ];
      })
    ) as DetailedEstimateAnchors['interior_rooms'],
  };
}
```

- [ ] Step 4: Add the type imports and Zod schema.

In lib/rate-settings.ts, add:

```ts
import {
  buildDefaultDetailedEstimateAnchors,
  DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS,
  type DetailedEstimateAnchors,
} from '@/lib/detailed-estimate-anchors';
```

Add a Zod schema:

```ts
const detailedEstimateAnchorRangeSchema = z.object({
  min: z.number().int().min(0),
  median: z.number().int().min(0),
  max: z.number().int().min(0),
});

const detailedEstimateAnchorsSchema = z.object({
  interior_rooms: z.record(z.string(), detailedEstimateAnchorRangeSchema),
});
```

Add detailed_estimate_anchors to UserRateSettings and ratePresetSchema.

- [ ] Step 5: Add defaults and parser merge.

In buildDefaultRateSettings:

```ts
settings.detailed_estimate_anchors = buildDefaultDetailedEstimateAnchors();
```

In parseUserRateSettings, merge valid provided room anchors into the default
object and ignore unknown entries by iterating DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS:

```ts
if (parsed.data.detailed_estimate_anchors?.interior_rooms) {
  for (const room of DETAILED_ESTIMATE_INTERIOR_ROOM_KEYS) {
    const range = parsed.data.detailed_estimate_anchors.interior_rooms[room];
    if (range) {
      result.detailed_estimate_anchors.interior_rooms[room] = range;
    }
  }
}
```

- [ ] Step 6: Run tests.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run lib/rate-settings.test.ts
```

Expected: PASS.

## Task 2: Make Interior Estimate Use Custom Room Anchors

Files:
- Modify: lib/interior-estimates.ts
- Test: lib/interior-estimates.test.ts

- [ ] Step 1: Write failing calculation tests.

Add tests:

```ts
import { calculateInteriorEstimate } from '@/lib/interior-estimates';
import { buildDefaultRateSettings } from '@/lib/rate-settings';

it('uses custom detailed estimate room anchors for specific areas', () => {
  const rates = buildDefaultRateSettings();
  rates.detailed_estimate_anchors.interior_rooms['Bedroom 1'] = {
    min: 200000,
    median: 200000,
    max: 200000,
  };

  const estimate = calculateInteriorEstimate(
    {
      property_type: 'apartment',
      estimate_mode: 'specific_areas',
      condition: 'excellent',
      scope: ['walls', 'ceiling', 'trim'],
      wall_paint_system: 'repaint_2coat',
      property_details: {},
      rooms: [{
        name: 'Bedroom',
        anchor_room_type: 'Bedroom 1',
        room_type: 'interior',
        length_m: null,
        width_m: null,
        height_m: null,
        include_walls: true,
        include_ceiling: true,
        include_trim: true,
      }],
      opening_items: [],
      trim_items: [],
    },
    rates
  );

  expect(estimate.pricing_items[0]?.unit_price_cents).toBeGreaterThan(150000);
});

it('does not use room flat rate presets for detailed estimates', () => {
  const rates = buildDefaultRateSettings();
  rates.room_rate_presets = [{
    id: 'preset-bedroom',
    title: 'Bedroom 1',
    sqm: 12,
    rate_cents: 999999,
  }];

  const estimate = calculateInteriorEstimate(
    {
      property_type: 'apartment',
      estimate_mode: 'specific_areas',
      condition: 'excellent',
      scope: ['walls', 'ceiling', 'trim'],
      wall_paint_system: 'repaint_2coat',
      property_details: {},
      rooms: [{
        name: 'Bedroom',
        anchor_room_type: 'Bedroom 1',
        room_type: 'interior',
        length_m: null,
        width_m: null,
        height_m: null,
        include_walls: true,
        include_ceiling: true,
        include_trim: true,
      }],
      opening_items: [],
      trim_items: [],
    },
    rates
  );

  expect(estimate.pricing_items[0]?.unit_price_cents).not.toBe(999999);
});
```

- [ ] Step 2: Run the focused test and confirm it fails.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run lib/interior-estimates.test.ts
```

Expected: FAIL because custom detailed anchors are not used yet.

- [ ] Step 3: Add a room-anchor resolver.

In lib/interior-estimates.ts, add a helper near calculateSpecificAreasEstimate:

```ts
function getSpecificAreaRoomAnchor(
  propertyType: InteriorPropertyType,
  roomType: InteriorRoomType,
  userRates?: UserRateSettings | null
) {
  const custom = userRates?.detailed_estimate_anchors?.interior_rooms?.[roomType];
  if (custom) {
    return {
      min: custom.min,
      median: custom.median,
      max: custom.max,
      unit: 'cents' as const,
    };
  }

  const defaults =
    propertyType === 'apartment'
      ? INTERIOR_ESTIMATE_ANCHORS.apartment.specific_areas.rooms
      : INTERIOR_ESTIMATE_ANCHORS.house.specific_areas.rooms;
  const fallback = defaults[roomType];

  return {
    min: fallback.min * 100,
    median: fallback.median * 100,
    max: fallback.max * 100,
    unit: 'cents' as const,
  };
}
```

- [ ] Step 4: Replace direct room anchor access.

Inside calculateSpecificAreasEstimate, replace:

```ts
const roomAnchors = input.property_type === 'apartment'
  ? INTERIOR_ESTIMATE_ANCHORS.apartment.specific_areas.rooms
  : INTERIOR_ESTIMATE_ANCHORS.house.specific_areas.rooms;
```

and:

```ts
const anchor = roomAnchors[room.anchor_room_type];
const itemMedian = cap(anchor.median * 100 * scopeMultiplier * storey_multiplier * conditionMedian * surfaceRateMultiplier);
```

with:

```ts
const anchor = getSpecificAreaRoomAnchor(input.property_type, room.anchor_room_type, userRates);
const itemMedian = cap(anchor.median * scopeMultiplier * storey_multiplier * conditionMedian * surfaceRateMultiplier);
```

Also use anchor.min and anchor.max without multiplying by 100.

- [ ] Step 5: Run tests.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run lib/interior-estimates.test.ts lib/rate-settings.test.ts
```

Expected: PASS.

## Task 3: Add Price Rates UI For Detailed Estimate Anchors

Files:
- Modify: components/rates/PriceRatesForm.tsx
- Create: components/rates/PriceRatesForm.test.tsx

- [ ] Step 1: Create a focused PriceRatesForm test file.

Create components/rates/PriceRatesForm.test.tsx with a basic render setup that
imports PriceRatesForm and buildDefaultRateSettings:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
import { PriceRatesForm } from '@/components/rates/PriceRatesForm';
import { buildDefaultRateSettings } from '@/lib/rate-settings';

describe('PriceRatesForm detailed estimate anchors', () => {
  it('shows detailed estimate anchor settings separately from room flat rate presets', () => {
    render(<PriceRatesForm defaultRates={buildDefaultRateSettings()} />);

    expect(screen.getByText('Detailed Estimate Anchors')).toBeInTheDocument();
    expect(screen.getByText(/Room flat rate presets stay separate/i)).toBeInTheDocument();
  });
});
```

- [ ] Step 2: Run the focused UI test and confirm it fails.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run components/rates/PriceRatesForm.test.tsx
```

Expected: FAIL because the Detailed Estimate Anchors section does not exist yet.

- [ ] Step 3: Add a method tab/section.

Use the existing hybrid tab as the Detailed Estimate settings area. Do not add a
new top-level pricing method.

Change the hybrid description in METHOD_DESCRIPTIONS:

```ts
hybrid: 'Detailed estimate anchors and rates',
```

- [ ] Step 4: Add RoomAnchors editor component.

Create a focused component inside PriceRatesForm.tsx:

```tsx
function DetailedEstimateAnchorsTab({
  rates,
  onChange,
}: {
  rates: UserRateSettings;
  onChange: (anchors: UserRateSettings['detailed_estimate_anchors']) => void;
}) {
  const rooms = Object.entries(rates.detailed_estimate_anchors.interior_rooms);

  return (
    <div className="space-y-4">
      <SectionHeading
        title="Detailed Estimate Anchors"
        subtitle="Set the base room prices used by Detailed Estimate. Room flat rate presets stay separate."
      />
      <div className="overflow-x-auto rounded-2xl border border-pm-border bg-white">
        <table className="w-full min-w-[420px] text-sm">
          <thead>
            <tr className="border-b border-pm-border bg-pm-surface">
              <th className="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-pm-secondary">Room</th>
              <th className="px-4 py-3 text-right text-xs font-semibold uppercase tracking-wide text-pm-secondary">Base price</th>
            </tr>
          </thead>
          <tbody>
            {rooms.map(([room, range]) => (
              <tr key={room} className="border-b border-pm-border last:border-0">
                <td className="px-4 py-3 font-medium text-pm-body">{room}</td>
                <td className="px-4 py-3 text-right">
                  <input
                    inputMode="numeric"
                    value={Math.round(range.median / 100)}
                    onChange={(event) => {
                      const dollars = Number(event.target.value || 0);
                      const median = Math.max(0, Math.round(dollars * 100));
                      onChange({
                        ...rates.detailed_estimate_anchors,
                        interior_rooms: {
                          ...rates.detailed_estimate_anchors.interior_rooms,
                          [room]: {
                            min: Math.round(median * 0.85),
                            median,
                            max: Math.round(median * 1.15),
                          },
                        },
                      });
                    }}
                    className="h-10 w-28 rounded-lg border border-pm-border bg-white px-3 text-right text-sm"
                  />
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

- [ ] Step 5: Wire the tab into state.

Add handler:

```ts
function handleDetailedEstimateAnchorsChange(
  detailed_estimate_anchors: UserRateSettings['detailed_estimate_anchors']
) {
  setRates((current) => ({ ...current, detailed_estimate_anchors }));
}
```

Render under activeTab === 'hybrid':

```tsx
{activeTab === 'hybrid' && (
  <DetailedEstimateAnchorsTab
    rates={rates}
    onChange={handleDetailedEstimateAnchorsChange}
  />
)}
```

- [ ] Step 6: Run focused UI test.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run components/rates/PriceRatesForm.test.tsx
```

Expected: PASS.

## Task 4: Add New Quote Guidance

Files:
- Modify: components/quotes/QuoteForm.tsx
- Test: components/quotes/QuoteForm.test.tsx

- [ ] Step 1: Write failing test for guidance text.

Add a test that renders QuoteForm with pricing method hybrid and expects:

```tsx
expect(screen.getByText(/Using Detailed Estimate Anchors from Price Rates/i))
  .toBeInTheDocument();
```

- [ ] Step 2: Add UI copy and link.

Near the Pricing Method helper area or Detailed Estimate builder, add:

```tsx
{pricingStrategy === 'hybrid' && (
  <p className="mt-2 text-xs text-pm-secondary">
    Using Detailed Estimate Anchors from{' '}
    <Link href="/settings/price-rates" className="font-medium text-pm-teal hover:text-pm-teal-hover">
      Price Rates
    </Link>
    .
  </p>
)}
```

Ensure Link is imported from next/link if not already imported.

- [ ] Step 3: Run focused test.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run components/quotes/QuoteForm.test.tsx
```

Expected: PASS.

## Task 5: Verify Server Create And Update Paths

Files:
- Modify: app/actions/quotes.test.ts

- [ ] Step 1: Add createQuote server test.

Mock getBusinessRateSettings to return custom detailed_estimate_anchors. Create a
hybrid interior estimate for Bedroom 1. Assert inserted quote subtotal reflects
the custom anchor and not the default.

Expected assertions:

```ts
expect(capturedQuoteInsert.subtotal_cents).toBeGreaterThan(150000);
expect(captured.estimateItemsInsert?.[0]).toEqual(
  expect.objectContaining({
    category: 'room_anchor',
    label: 'Bedroom',
  })
);
```

- [ ] Step 2: Add updateQuote server test.

Use the existing updateQuote test setup. Update a hybrid interior estimate and
assert the quote update uses the custom anchor subtotal.

- [ ] Step 3: Run action tests.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run app/actions/quotes.test.ts
```

Expected: PASS. If it fails because app/actions/quotes.ts bypasses the updated
calculator, route the calculation through calculateInteriorEstimate with
effectiveRates in both createQuote and updateQuote.

## Task 6: Regression Verification

Files:
- No code edits expected.

- [ ] Step 1: Run focused pricing and quote tests.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx vitest run lib/rate-settings.test.ts lib/interior-estimates.test.ts components/quotes/QuoteForm.test.tsx app/actions/quotes.test.ts app/api/pdf/quote/route.test.ts lib/quotes.test.ts
```

Expected: PASS.

- [ ] Step 2: Run typecheck.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx tsc --noEmit
```

Expected: PASS.

- [ ] Step 3: Run lint on touched files.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx eslint lib/rate-settings.ts lib/interior-estimates.ts components/rates/PriceRatesForm.tsx components/quotes/QuoteForm.tsx app/actions/quotes.ts
```

Expected: PASS.

- [ ] Step 4: Run formatting check.

Run:

```bash
PATH="/Users/jimee/.nvm/versions/node/v24.14.1/bin:$PATH" npx prettier --check lib/rate-settings.ts lib/interior-estimates.ts components/rates/PriceRatesForm.tsx components/quotes/QuoteForm.tsx app/actions/quotes.ts
```

Expected: PASS.

- [ ] Step 5: Browser QA.

Use the in-app browser at http://localhost:3000/settings/price-rates and
http://localhost:3000/quotes/new.

Verify:

- Price Rates shows Detailed Estimate Anchors.
- Changing a room base price saves.
- New Quote > Detailed estimate changes preview total after the setting is saved.
- Room flat rate presets still appear only under Room flat rate.
- Quote PDF and customer quote display saved estimate line items.

## Self-Review

Spec coverage:
- User-configurable detailed room anchors are covered in Tasks 1-3.
- New Quote calculation is covered in Tasks 2, 4, and 5.
- Room flat rate separation is covered in Tasks 2 and 3.
- Server-side calculation is covered in Task 5.
- Verification is covered in Task 6.

Placeholder scan:
- No placeholder markers remain.

Type consistency:
- detailed_estimate_anchors is the single setting name across type, parser, UI,
  and calculation tasks.
- Room anchor values are cents after normalization.

Execution choice:
- Recommended: Subagent-driven implementation, one task at a time, with review
  after each task.
- Alternative: Inline execution in this session using executing-plans.
